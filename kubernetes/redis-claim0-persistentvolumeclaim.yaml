apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: redis-claim0
  name: redis-claim0
  namespace: n8n
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi